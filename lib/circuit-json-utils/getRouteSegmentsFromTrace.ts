import type { PcbTrace } from "circuit-json"
import type { NormalizedObject, NormalizedTrace } from "../types"

export const getRouteSegmentsFromTrace = (
  trace: PcbTrace,
): NormalizedTrace["route_segments"] => {
  //
}
